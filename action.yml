name: 'XK6 Browser'
description: 'Run XK6 Browser tests'
inputs:
  testRegex:
    description: 'Where to look for tests'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.testRegex }}